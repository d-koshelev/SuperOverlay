<Window x:Class="SuperOverlay.iRacing.RuntimeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SuperOverlay Runtime"
        WindowStartupLocation="Manual"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        Focusable="True"
        KeyDown="Window_OnKeyDown">

    <Grid Background="Transparent">
        <Grid x:Name="RootGrid"
              Background="Transparent"
              PreviewMouseLeftButtonDown="RootGrid_OnMouseLeftButtonDown"
              MouseMove="RootGrid_OnMouseMove"
              MouseLeftButtonUp="RootGrid_OnMouseLeftButtonUp"/>

        <Border x:Name="RuntimeHintBorder"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Margin="12"
                Padding="10,6"
                Background="#CC111827"
                BorderBrush="#667085"
                BorderThickness="1"
                CornerRadius="8">
            <TextBlock Foreground="White" FontSize="12">
                <Run Text="Runtime"/>
                <Run Text=" • Ctrl+Shift+E move edit"/>
            </TextBlock>
        </Border>

        <Border x:Name="EditOverlayBar"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Margin="12"
                Padding="10,8"
                Background="#E61D4ED8"
                BorderBrush="White"
                BorderThickness="1"
                CornerRadius="8"
                Visibility="Collapsed">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Foreground="White" FontWeight="Bold" VerticalAlignment="Center" Text="Runtime move edit"/>
                <TextBlock Foreground="#E5E7EB" Margin="12,0,0,0" VerticalAlignment="Center" Text="Drag items to reposition. Resize and settings stay in builder."/>
                <Button x:Name="ApplyEditButton" Content="Apply" Margin="12,0,0,0" Padding="10,4" Click="ApplyEditButton_OnClick"/>
                <Button x:Name="CancelEditButton" Content="Cancel" Margin="6,0,0,0" Padding="10,4" Click="CancelEditButton_OnClick"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
